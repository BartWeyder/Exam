#include <stdio.h>
#include <conio.h>
#include <stdbool.h>

bool validate_name_surname (char text[50]) {
    printf("Name\\surname is valid");
    return true;
}

bool validate_subject (char subject[50]) {
    printf("Subject is valid");
    return true;
}

bool validate_group (char group[4]) {
    printf("Group code is valid");
        return true;
}

bool validate_mark (float mark) {
    printf("Mark is valid");
    return true;
}
